// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id            Int            @id @default(autoincrement())
  name          String
  email         String         @unique
  password      String
  birthday      DateTime?
  type          Type           @default(MENTEE)
  MentorProfile MentorProfile? @relation(name: "MentorProfileToUser")
  MenteeProfile MenteeProfile? @relation(name: "MenteeProfileToUser")

  @@map("users")
}

model MenteeProfile {
  id           Int     @id @default(autoincrement())
  profilePhoto String?
  nickname     String
  introduce    String
  user         User    @relation(name: "MenteeProfileToUser", fields: [userId], references: [id])
  userId       Int     @unique

  @@map("mentee_profiles")
}

model MentorProfile {
  id           Int     @id @default(autoincrement())
  profilePhoto String?
  nickname     String
  introduce    String
  review       String
  user         User    @relation(name: "MentorProfileToUser", fields: [userId], references: [id])
  userId       Int     @unique

  @@map("mentor_profiles")
}

enum Type {
  MENTEE
  MENTOR
}
