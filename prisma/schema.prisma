// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Account {
  id            Int            @id @default(autoincrement())
  nickName      String
  email         String         @unique
  password      String
  profilePhoto  String?
  introduce     String?
  role          Role           @default(MENTEE)
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  RefreshToken  RefreshToken?  @relation(name: "AccountRefreshToken")
  MentorProfile MentorProfile? @relation(name: "MentorAccount")
  Project       Project[]  
  @@map("accounts")
}

model RefreshToken {
  id           Int      @id @default(autoincrement())
  refreshToken String   @default("")
  account      Account  @relation(name: "AccountRefreshToken", fields: [accountId], references: [id])
  accountId    Int      @unique
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@map("refresh_tokens")
}

model MentorProfile {
  id              Int            @id @default(autoincrement())
  mentorIntroduce String         @db.VarChar(100)
  portfolioLink   String?
  major           String?
  account         Account        @relation(name: "MentorAccount", fields: [accountId], references: [id])
  accountId       Int            @unique
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
  ProtFolioImg    ProtFolioImg[] @relation(name: "MentorPortfolioImg")
  Skill           Skill[]        @relation(name: "MentorProfileToSkill")

  @@map("mentor_profiles")
}

model ProtFolioImg {
  id              Int           @id @default(autoincrement())
  url             String
  mentorProfile   MentorProfile @relation(name: "MentorPortfolioImg", fields: [mentorProfileId], references: [id])
  mentorProfileId Int
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
}

model Skill {
  id            Int             @id @default(autoincrement())
  name          String
  mentorProfile MentorProfile[] @relation(name: "MentorProfileToSkill")
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
  projectId     Int?
  Project       StacksOnProject[]         
}

model Project {
  id              Int                     @id @default(autoincrement())
  title           String
  startDate       DateTime
  endDate         DateTime
  memberInfo      String?
  projectDetail   String
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt
  skillId         Int?
  accountId       Int
  skill           StacksOnProject[]
  Account         Account                 @relation(fields: [accountId], references: [id])
  Categories      CategoriesOnProject[]
}

model Category {
  id              Int                     @id @default(autoincrement())
  name            String
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt
  project         CategoriesOnProject[]
}


model CategoriesOnProject {
  id              Int             @id @default(autoincrement())
  projectId       Int
  project         Project         @relation(fields: [projectId], references: [id])
  categoryId      Int
  category        Category        @relation(fields: [categoryId], references: [id])
}

model StacksOnProject {
  id              Int             @id @default(autoincrement())
  projectId       Int
  project         Project         @relation(fields: [projectId], references: [id])
  skillId         Int
  skill           Skill           @relation(fields: [skillId], references: [id])
}


enum Role {
  MENTEE
  MENTOR
}
