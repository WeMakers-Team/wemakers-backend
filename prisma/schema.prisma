// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Account {
  id            Int            @id @default(autoincrement())
  nickName      String
  email         String         @unique
  password      String
  profilePhoto  String?
  introduce     String?
  role          Role           @default(MENTEE)
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  RefreshToken  RefreshToken?  @relation(name: "AccountRefreshToken")
  MentorProfile MentorProfile? @relation(name: "MentorAccount")

  @@map("accounts")
}

model RefreshToken {
  id           Int      @id @default(autoincrement())
  refreshToken String   @default("")
  account      Account  @relation(name: "AccountRefreshToken", fields: [accountId], references: [id])
  accountId    Int      @unique
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@map("refresh_tokens")
}

model MentorProfile {
  id              Int      @id @default(autoincrement())
  mentorIntroduce String   @db.VarChar(100)
  portfolioLink   String?
  major           String?
  company         String?
  isPublic        Boolean  @default(true)
  account         Account  @relation(name: "MentorAccount", fields: [accountId], references: [id])
  accountId       Int      @unique
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  Skill           Skill[]  @relation(name: "MentorProfileToSkill")

  @@map("mentor_profiles")
}

model Skill {
  id            Int             @id @default(autoincrement())
  name          String
  logo          String
  mentorProfile MentorProfile[] @relation(name: "MentorProfileToSkill")
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
}

enum Role {
  MENTEE
  MENTOR
}
