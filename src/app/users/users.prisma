model User {
  id           Int       @id @default(autoincrement())
  name         String
  email        String    @unique
  password     String
  birthday     DateTime?
  refreshToken String    @default("")
  role         Role      @default(MENTEE)

  @@map("users")
}

enum Role {
  MENTEE
  MENTOR
}
